# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real credentials.

NET_USERNAME=your_ssh_username
NET_PASSWORD=your_ssh_password

# Optional: enable password for privileged/enable mode on Cisco devices
NET_ENABLE_SECRET=your_enable_secret
